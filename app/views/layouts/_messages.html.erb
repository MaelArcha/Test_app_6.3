<div class="container">
  <% flash.each do |name, msg | %>
    <div class="alert <%= flash_class(name)%> dismissable fade show"role="alert">
      <%= msg %>
      <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>  
  <% end %>
</div>
